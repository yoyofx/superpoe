# Include this inside the existing HTTPS server block for the auth domain.
# Keep the service bound to 127.0.0.1; Nginx is the only public listener.
location /api/ {
    limit_req zone=superpoe_auth burst=20 nodelay;
    proxy_pass http://127.0.0.1:8787;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 30s;
    proxy_send_timeout 30s;
}

# Add this once in the http {} block of nginx.conf:
# limit_req_zone $binary_remote_addr zone=superpoe_auth:10m rate=10r/s;
